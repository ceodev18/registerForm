<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kelly.registerform">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.MainActivity">

        </activity>
        <activity android:name=".view.partner.RegistrationPartnerActivity" />
        <activity android:name=".view.partner.RegistrationPartnerPartBActivity" />
        <activity android:name=".view.partner.WelcomePartnerActivity" />
        <activity android:name=".view.farming.ProductionActivity" />
        <activity android:name=".view.partner.ValidationActivity" />
        <activity android:name=".view.commerce.InformationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>